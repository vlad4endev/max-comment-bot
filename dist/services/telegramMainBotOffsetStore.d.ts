/** Единый offset getUpdates для основного TG-бота (связки + mini app + discovery). */
export declare function getTelegramBotUpdatesOffset(token: string): number;
export declare function setTelegramBotUpdatesOffset(token: string, offset: number): void;
